import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "www.google.com",
      "www.facebook.com",
      "twitter.com",
      "github.com",
      "www.linkedin.com"
    ]
  }
  /* config options here */
};

export default nextConfig;
